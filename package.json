{
  "name": "smart-model-router",
  "version": "0.1.0",
  "description": "llm-cost-router-for-saas: API gateway",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "find src -name '*.test.ts' -print0 | xargs -0 node --require ts-node/register --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.0"
  }
}
